pandas>=2.0
numpy>=1.26
scipy>=1.11
xgboost>=2.0
scikit-learn>=1.4
requests>=2.31
joblib>=1.3
# (pybaseball is no longer needed — pitcher stats now come from the same
# free MLB Stats API as the schedule.)
