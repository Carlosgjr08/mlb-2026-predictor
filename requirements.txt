pandas>=2.0
numpy>=1.26
scipy>=1.11
xgboost>=2.0
scikit-learn>=1.4
requests>=2.31
joblib>=1.3
# Optional — real starting-pitcher stats (xFIP, K-BB%) for `fetch`.
# Without it, starters fall back to league average. `pip install pybaseball`
# pybaseball>=2.2
